apiVersion: v1
kind: Service
metadata:
  name: {{ include "react-ruby.fullname" . }}
  labels:
    {{- include "react-ruby.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "react-ruby.selectorLabels" . | nindent 4 }}
